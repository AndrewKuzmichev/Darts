$(document).ready(function() {
	$("#count").click(function(event){
		// $.ajax('/playground',function(data){
		// 	console.log(data);
		// });


		/*
		var sum = 0;
		$("#right_playground input").each(function(){
			var val = parseInt( $(this).val() );
			sum = sum + val;
		});
		console.log( $("#nowPlayer").text() );
		//$.get('/playground',{ sum:sum });
		*/
	});


});// Конец ready
